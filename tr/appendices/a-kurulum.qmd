# Ortam Kurulumu {#sec-kurulum}

::: {.callout-note appearance="simple" icon="false"}
**Ek durumu: çevrildi.** Kaynak metin:
[İngilizce A eki](../../en/appendices/a-setup.html). Buradaki sürümler kitabın
ölçümlerinin gerçekten koşulduğu sürümler; böylece bir bölümün sayıları
yaklaşık olarak değil, gerçekten yeniden üretilebilir oluyor.
:::

Bu kitaptaki her şey üç yerden birinde koşuyor:

- **Tarayıcınızda.** Her etkileşimli araç kendi kendine yeterli — kurulacak bir
  şey yok, koşulacak bir şey yok.
- **Bir Python ortamında.** Çalıştırılabilir bölümler Python ≥ 3.11 ve kısa bir
  bağımlılık listesi istiyor.
- **Yerel bir model sunucusunda.** IV–VII. kısımlar bir model çağırıyor. O
  kısımlardaki her ölçüm, barındırılan bir API yerine bir dizüstünde `ollama`
  kullandı; böylece sayıların yeniden üretilmesi hiçbir şeye mal olmuyor ve bir
  sağlayıcının o anki model sürümüne bağlı olmuyor.

## Yerel kurulum {#sec-kurulum-yerel}

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```

Ya da daha hızlı olan ve aynı sürümleri çözen `uv` ile:

```bash
uv venv && source .venv/bin/activate
uv pip install -r requirements.txt
```

Kitabın ölçümlerinin kullandığı ve `requirements.txt`in sabitlediği sürümler:

| paket | sürüm | nerede kullanılıyor |
|:--|:--|:--|
| `numpy` | 2.5.2 | sayısal olan neredeyse her şey |
| `scikit-learn` | 1.8.2 | @sec-karsilastirma, @sec-siniflandirma |
| `scipy` | 1.18.1 | @sec-llm-degerlendirme'nin boyutlandırma aritmetiği |
| `transformers` | 5.15.1 | @sec-dizi-etiketleme, @sec-makine-cevirisi, @sec-turkce-nlp |
| `torch` | 2.13.0 | o bölümlerin ince ayarladığı modeller |
| `datasets` | 5.0.1 | kitabın yüklediği her derlem |
| `sacrebleu` | 2.6.0 | @sec-makine-cevirisi |
| `snowballstemmer` | 3.1.1 | @sec-turkce-nlp |
| `rouge-score` | 0.1.2 | @sec-ozetleme — güvenmeden önce o bölümü okuyun |
| `matplotlib` | 3.11.1 | kendiniz yeniden ürettiğiniz şekiller |
| `jupyter` | 1.1.1 | not defterleri |

: Ölçüldüğü hâliyle sabitlenmiş sürümler {tbl-colwidths="[26,14,60]"}

[En sonuncusunu almak yerine bunları sabitleyin.]{.hl} Kitabın bulgularından
birkaçı belirli alet davranışlarıyla ilgili — @sec-ozetleme'nin ROUGE
tokenlaştırıcı hatası, @sec-turkce-nlp'nin küçük harfe çevirme arızaları — ve
daha yeni bir sürüm, bir bölümün gösterdiği şeyi düzeltmiş olabilir. Bu, dünya
için iyi bir sonuç ve alıştırmanın ortasında kafa karıştırıcı bir sonuç.

## Yerel bir model {#sec-kurulum-ollama}

IV–VII. kısımlar çağrılacak bir modele ihtiyaç duyuyor.
[ollama](https://ollama.com)'yı kurun, sonra:

```bash
ollama pull qwen2.5:7b
ollama serve
```

Kitap `ollama` 0.24.0 kullandı. Her bölümün kullandığı modeller o bölümün durum
kutusunda adlandırılıyor; örnekleri koşturacak en küçüğü `llama3.2:3b` ve genel
varsayılan `qwen2.5:7b`. @sec-yonetisim'de 27B'lik bir model kullanıldı ve
kabaca 20 GB belleğe ihtiyacı var — bu kadarınız yoksa atlayın, bölümün savı
en üst basamak olmadan da ayakta kalıyor.

Her şey doğrudan HTTP API'siyle konuşuyor:

```bash
curl -s http://127.0.0.1:11434/api/generate \
  -d '{"model":"qwen2.5:7b","prompt":"Merhaba","stream":false}'
```

::: {.column-margin}
Bir şeyi bir saat boyunca ayıklamadan önce bilinmeye değen iki bayrak.
`"format": "json"` ayrıştırılabilir çıktıyı zorluyor ve bütün bir puanlama
hatası sınıfını ortadan kaldırıyor. Ve `"think": false` akıl yürüten modeller
için önemli — o olmadan `ollama` boş bir `response` döndürüyor ve içerik ayrı
bir `thinking` alanında kalıyor, ki bu sıfır olarak puanlanıyor ve felaket bir
kalite sonucu gibi görünüyor. @sec-yonetisim'in ölçümü tam olarak buna çarptı.
:::

## Colab {#sec-kurulum-colab}

Çalıştırılabilir her bölüm not defterini bir *Open in Colab* rozetiyle
bağlıyor — sıfır yerel kurulum ve bir bölüm gerektirdiğinde ücretsiz bir GPU.
I–III. kısımların ihtiyacı yok. Yerel model kullanan bölümler Colab'ın ücretsiz
katmanında uyarlanmadan çalışmıyor; yerine barındırılan bir API koyun ve
sayılarınızın kitabınkilerden farklı olmasını bekleyin, ki bu da sabitleme
hakkında başlı başına yararlı bir ders.

## Windows {#sec-kurulum-windows}

Her şey çalışıyor; üç şey farklı.

Sanal ortamı `source` yerine `.venv\Scripts\activate` ile etkinleştirin. Bir şey
sizinle didişiyorsa **WSL2** kullanın — kitabın ölçümleri macOS ve Linux'ta
koşuldu ve onlara ulaşmanın en kısa yolu WSL2. Ve `PYTHONUTF8=1` ayarlayın,
çünkü Windows hâlâ bazı dosya okumalarında UTF-8 olmayan bir kodlamaya
düşüyor, ki bu da Türkçe metni sessizce bozuyor. Sonuncusu,
@sec-turkce-nlp'nin savının siz daha bir model yüklemeden gelmesi.

## Yardım almak {#sec-kurulum-yardim}

Başlıkta bölüm adıyla bir GitHub sorunu açın ya da herhangi bir sayfanın
altındaki yorum kutusunu kullanın. Kitaptaki bir sayı yeniden üretilmiyorsa
bu bir soruna değer — hangi bölüm, hangi sürüm sizde ve ne aldığınızı
söyleyin.
