# Dependencies for running the book's chapter code.
#
# Every version here is the one a chapter's own "The stack these numbers came
# from" table reports. Where two chapters disagree, the later version is pinned
# and the disagreement is noted in Appendix A.
#
# This is NOT the file that builds the book — that is requirements.txt.
#
# Python 3.14.5 is the interpreter every measurement ran on.

numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.18.1
imbalanced-learn==0.14.2

transformers==5.15.1
torch==2.13.0
datasets==5.0.1
sentence-transformers==5.7.0

sacrebleu==2.6.0
seqeval==1.2.2
sklearn-crfsuite==0.5.0
snowballstemmer==3.1.1
rouge-score==0.1.2

datasketch==2.0.0
umap-learn==0.5.12

ollama==0.24.0
pytest==9.1.1
