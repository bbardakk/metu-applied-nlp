# Math Refresher {#sec-math-refresher}

::: {.callout-note appearance="simple" icon="false"}
**Appendix status: skeleton.** Target: every mathematical fact the book
uses, each with a two-line refresher and a pointer to where it is used.
:::

## Linear algebra

<!-- TODO: vectors as data points; dot product as similarity; matrix
     multiplication as batched dot products; SVD in one figure
     (→ @sec-embeddings). -->

## Probability

<!-- TODO: conditional probability and the chain rule (→
     @sec-language-models); expectation; cross-entropy and perplexity;
     softmax as exponential reweighting (→ @sec-transformers). -->

## Optimization

<!-- TODO: gradient descent in one picture; what "training" means
     operationally; regularization as prior belief. -->

## Notation and symbols {#sec-notation}

<!-- TODO: one table of every symbol the book uses — symbol, what it
     means, and the chapter where it first appears. Cover at minimum:
     vectors and matrices (bold vs. plain), the dot product, |V| for
     vocabulary size, d for embedding dimension, θ for parameters, and
     the softmax and expectation operators. Keep it a plain table; the
     sticky sidebar leaves no room for wide layouts.

     This lives inside the math refresher on purpose — a symbol is only
     useful next to the idea it denotes, and a standalone notation
     appendix would duplicate the sections above. -->
