# Text as Data: Tokens, Normalization, Zipf {#sec-text-as-data}

::: {.callout-note appearance="simple" icon="false"}
**Chapter status: skeleton.**
:::

**Learning objectives.** After this chapter you can:

- turn raw text into model-ready units and defend each preprocessing choice;
- explain byte-pair encoding well enough to predict how a tokenizer splits an unseen word;
- describe Zipf's law and its practical consequence: the vocabulary problem;
- explain why tokenization is where multilingual pain (including Turkish morphology) begins.

## From characters to tokens

<!-- TODO(draft): whitespace vs subword; the BPE training loop as a
     compression algorithm; worked example segmenting "unbelievably" and a
     Turkish agglutinative example ("evlerimizden") — this doubles as a
     hook for the future Turkish edition. -->

The chips on this book's cover page are not a metaphor: modern models
consume [subword tokens]{.hl}, and every downstream cost — context limits,
API pricing, multilingual inequity — is denominated in them.

## Normalization decisions and their consequences

<!-- TODO(draft): casing, Unicode, stemming vs lemmatization; when
     aggressive normalization destroys signal. Statistician framing:
     preprocessing is part of the model. -->

## Zipf's law and the long tail

<!-- TODO(draft): plot rank–frequency on a real corpus (make executable,
     matplotlib, freeze). Consequence: OOV, why subwords won. -->

## Exercises

1. Tokenize the same paragraph with two different tokenizers and compare
   token counts. Who pays more for Turkish, and why?
