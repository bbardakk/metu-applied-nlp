# Why NLP, Why Now {#sec-why-nlp}

::: {.callout-note appearance="simple" icon="false"}
**Chapter status: drafted.** The timeline and the discipline explorer below
are fully interactive.
:::

**Learning objectives.** After this chapter you can:

- describe what changed between 2017 and today that made language *the* interface to computation;
- place classical NLP, deep learning NLP, and LLMs on one historical map;
- name three problems in your own discipline that are now NLP problems;
- state honestly what these systems still cannot do;
- choose a reading path through this book.

## Language became an interface

Here is a question worth sitting with before any mathematics. Your
organization has spent decades building databases: columns, types,
constraints, foreign keys. Enormous care went into deciding that a
temperature is a `float` and a batch ID is a `varchar(12)`. And yet, if you
ask where the *knowledge* in your organization actually lives, the honest
answer is rarely the database. It lives in shift handover notes, incident
reports, supplier emails, standards documents, maintenance logs, the
"comments" column nobody analyzes, and a decade of PDFs.

For most of computing history that material was called [unstructured
data]{.hl} — a polite way of saying *we have no idea what to do with it*. You
could count words in it. You could search it with keywords, badly. Anything
more required a human to read it.

The shortest honest summary of the last decade is that this stopped being
true. Reports, logs, contracts, sensor annotations, scientific literature —
the material your field already produces — became computable. Not perfectly,
not without supervision, but well enough that "have a machine read all
40,000 of these and tell me which ones mention a seal failure" moved from a
research project to an afternoon's work.

This chapter is about how that happened, why it happened *now*, and what it
means for someone who did not set out to become a computer scientist.

::: {.column-margin}
This book assumes you are here because text showed up in *your* pipeline —
not because you want to build the next frontier model. Every chapter is
written from that angle.
:::

## Seventy years in one figure

NLP is not new. It is one of the oldest ambitions in computing, and it
failed publicly and repeatedly for fifty years before it worked. That
history is worth knowing, because it tells you which claims to be
suspicious of.

::: {.anlp-widget}
```{=html}
<div class="widget-title">Seventy years of NLP — pick an era to read its milestones</div>
```

```{ojs}
//| echo: false
viewof eraChoice = Inputs.radio(
  ["All eras",
   "1 · Rules (1950–1990)",
   "2 · Statistics (1990–2012)",
   "3 · Neural (2013–2017)",
   "4 · Pretraining (2018–2022)",
   "5 · Reasoning & agents (2023–)"],
  { value: "All eras", label: "" }
)
```

```{ojs}
//| echo: false
milestones = [
  { year: 1954, era: "1 · Rules", label: "Georgetown–IBM demo",
    note: "Sixty Russian sentences translated by machine. The press reported that translation would be solved within five years." },
  { year: 1966, era: "1 · Rules", label: "ELIZA, and the ALPAC report",
    note: "Weizenbaum's ELIZA convinced people it understood them using a few dozen pattern-matching rules. The same year, the ALPAC report judged machine translation a failure and the funding collapsed." },
  { year: 1972, era: "1 · Rules", label: "SHRDLU",
    note: "Genuinely impressive language understanding — inside a toy world of coloured blocks. Nothing about the approach scaled beyond it." },
  { year: 1985, era: "1 · Rules", label: "Hand-written grammars",
    note: "Linguists encoded syntax by hand. Coverage grew linearly with effort, and real text kept breaking the rules faster than they could be written." },
  { year: 1993, era: "2 · Statistics", label: "IBM models, Penn Treebank",
    note: "Translation is reframed as parameter estimation from parallel text, and a large annotated corpus arrives. The field starts counting instead of legislating." },
  { year: 1998, era: "2 · Statistics", label: "n-gram models everywhere",
    note: "Speech recognition, spelling correction and search all run on counting word sequences. Simple, effective, and completely blind to meaning." },
  { year: 2003, era: "2 · Statistics", label: "Neural language model",
    note: "Bengio and colleagues predict the next word with a neural network over learned word vectors — the entire modern recipe, twenty years early and starved of compute." },
  { year: 2013, era: "3 · Neural", label: "word2vec",
    note: "Word meaning becomes geometry: dense vectors trained on raw text, no annotation required. Suddenly 'similar' is something you can compute." },
  { year: 2014, era: "3 · Neural", label: "seq2seq, then attention",
    note: "Encoder–decoder networks translate end-to-end, and attention removes the fixed-size bottleneck by letting the decoder look back at any input word." },
  { year: 2017, era: "3 · Neural", label: "The Transformer",
    note: "Attention alone, no recurrence — so training parallelizes across the whole sequence. This is the architectural unlock; most of what follows is scale and refinement." },
  { year: 2018, era: "4 · Pretraining", label: "ELMo, GPT-1, BERT",
    note: "Pretrain once on unlabeled text, fine-tune on your small labeled set. Benchmark records fall within months and per-task architectures start disappearing." },
  { year: 2020, era: "4 · Pretraining", label: "GPT-3 and scaling laws",
    note: "Performance turns out to be a smooth, predictable function of compute, data and parameters — and a large enough model learns a new task from examples in the prompt, with no gradient updates at all." },
  { year: 2022, era: "4 · Pretraining", label: "Instruction tuning, then ChatGPT",
    note: "Training on human preferences turns a text predictor into something that follows instructions. In November the general public meets it, and the interface question is settled." },
  { year: 2023, era: "5 · Reasoning", label: "Multimodal, and open weights",
    note: "Models read images and documents, not just text; capable open-weight models make on-premise deployment realistic for regulated industries." },
  { year: 2024, era: "5 · Reasoning", label: "Long context, cost collapse",
    note: "Context windows grow from pages to books, and the price of a given level of capability falls by orders of magnitude — which is what turns demos into products." },
  { year: 2025, era: "5 · Reasoning", label: "Reasoning models and tool use",
    note: "Models spend extra computation at answer time on hard problems, and call tools, run code and query databases instead of guessing." }
]
```

```{ojs}
//| echo: false
shown = eraChoice === "All eras"
  ? milestones
  : milestones.filter(d => d.era.startsWith(eraChoice.slice(0, 1)))
```

```{ojs}
//| echo: false
eraOrder = ["1 · Rules", "2 · Statistics", "3 · Neural", "4 · Pretraining", "5 · Reasoning"]
```

```{ojs}
//| echo: false
Plot.plot({
  width: 680,
  height: 250,
  marginLeft: 116,
  marginBottom: 40,
  x: { domain: [1950, 2028], label: "year", tickFormat: "d", grid: true },
  y: { domain: eraOrder, label: null },
  color: {
    domain: eraOrder,
    range: ["#C08A3E", "#6B7280", "#0E7C7B", "#1F4E79", "#B3453E"],
    legend: false
  },
  marks: [
    Plot.ruleY(eraOrder, { y: d => d, stroke: "#E3E7EC" }),
    Plot.dot(milestones, { x: "year", y: "era", r: 4, fill: "#DDE2E7" }),
    Plot.dot(shown, {
      x: "year", y: "era", r: 6.5, fill: "era", stroke: "white", strokeWidth: 1.5,
      title: d => `${d.year} — ${d.label}`
    })
  ]
})
```

```{ojs}
//| echo: false
html`<div style="font-size:0.9rem; line-height:1.55;">
  ${shown.map(d => html`<p style="margin:0 0 0.6rem 0;">
    <b style="font-variant-numeric:tabular-nums;">${d.year}</b>
    &nbsp;·&nbsp; <b>${d.label}</b><br>
    <span style="color:#5B6272;">${d.note}</span>
  </p>`)}
</div>`
```
:::

::: {#gloss-language-model .column-margin .term-gloss}
**language model** — a program that guesses the next word. That is all it
is trained to do, and everything else these systems appear to do is a
side effect of doing it well. [↩](#term-language-model){.term-back}
:::

Two things stand out on that timeline. First, [the ideas arrived decades
before they worked]{.hl}. Neural [language models](#gloss-language-model){#term-language-model .term-ref} were described in 2003
[@bengio2003neural]; they became useful once there was enough compute and
text to feed them. Second, each era did not politely replace the last one —
it absorbed it. Counting still underpins tokenization (@sec-text-as-data),
and the 2003 recipe is still, structurally, what a modern LLM does.

## Three shifts that actually mattered

Behind that timeline are three distinct changes. They are worth separating,
because people routinely credit the most recent one for the work of the
first two.

### From writing rules to estimating parameters

The rule-based era failed for a reason a statistician will find obvious:
language has an enormous tail. Whatever grammar you write, real text will
violate it by tomorrow afternoon. The statistical turn of the 1990s
[@brown1993mathematics] replaced *legislating* language with *estimating* it
from data — and essentially every improvement since has come from estimating
more parameters from more data.

Rich Sutton's essay *The Bitter Lesson* [@sutton2019bitter] is the sharpest
statement of this pattern: across seventy years, methods that leverage
computation have beaten methods that encode human insight, every time — and
the researchers encoding insight were always confident that this time would
be different.

### From one model per task to one model, many tasks

This is the shift that changed the economics, and it is the one most people
miss.

```{=html}
<div class="part-grid">
  <div class="part-card">
    <div class="kicker">Before ~2018</div>
    <h3>One task, one pipeline</h3>
    <ul>
      <li>Collect 10k–100k labeled examples <b>per task</b></li>
      <li>Design task-specific features and architecture</li>
      <li>Train from scratch; it transfers to nothing</li>
      <li>New task next quarter → start over</li>
    </ul>
  </div>
  <div class="part-card">
    <div class="kicker">After</div>
    <h3>One pretrained model, many tasks</h3>
    <ul>
      <li>Pretrain once on unlabeled text (someone else already paid)</li>
      <li>Adapt with a few hundred examples — or none</li>
      <li>The same model serves classification, extraction, generation</li>
      <li>New task next quarter → a new prompt</li>
    </ul>
  </div>
</div>
```

::: {#gloss-pretraining .column-margin .term-gloss}
**pretraining** — the long, expensive first training run on ordinary
unlabelled text, done once by someone else. What you do afterwards is
cheap because of it. [↩](#term-pretraining){.term-back}
:::

[Pretraining](#gloss-pretraining){#term-pretraining .term-ref} [@devlin2019bert] moved the expensive part of the work upstream
and made it reusable. That is why a five-person team in a chemical plant can
now attempt something that required a research group in 2015: the
labeled-data requirement — historically the reason NLP projects died in the
proposal stage — fell by two or three orders of magnitude.

::: {#gloss-prompt .column-margin .term-gloss}
**prompt** — the text you send the model, instructions and examples
included. With a large enough model the prompt does the job that
retraining used to do. [↩](#term-prompt){.term-back}
:::

GPT-3 [@brown2020language] then demonstrated the extreme version. Describe
the task in the [prompt](#gloss-prompt){#term-prompt .term-ref}, give two or three examples, and the model performs
it with *no parameter updates at all*. We take this for granted now; it was
genuinely startling in 2020.

### From "train a model" to "write an instruction"

The final shift is about interface. A pretrained model is a powerful but
awkward text-continuation engine. Training it on human preferences
[@ouyang2022training] turned it into something that follows instructions —
and at that moment the user interface to a machine learning model became
*a sentence in your own language*.

That is the precise sense in which language became an interface. The people
who know which maintenance logs matter can now question them directly,
without a translation layer of engineers in between. Whether that is a good
thing depends entirely on whether the answers are correct — which is why
@sec-llm-eval exists.

::: {.callout-tip appearance="simple"}
**Why now, in one sentence.** The architecture arrived in 2017, pretraining
made it reusable in 2018, scale made it general in 2020, instruction tuning
made it usable in 2022 — and the collapse in inference cost since then made
it cheap enough to put into production. Stanford's AI Index tracks that last
point: the cost of serving GPT-3.5-level quality fell roughly 280-fold in
about two years [@aiindex2026].
:::

## Your discipline is already an NLP problem

Six task families cover almost everything people actually build:
classification, retrieval, extraction, generation, agents, and evaluation.
Pick your field below to see which of your documents map to which family —
and which chapter covers it.

::: {.anlp-widget}
```{=html}
<div class="widget-title">Where does your work meet NLP?</div>
```

```{ojs}
//| echo: false
viewof fieldChoice = Inputs.select(
  ["Chemical & process engineering",
   "Civil & structural engineering",
   "Industrial & manufacturing engineering",
   "Energy & environmental engineering",
   "Statistics & social science"],
  { label: "Your field", value: "Chemical & process engineering" }
)
```

```{ojs}
//| echo: false
useCases = [
  { field: "Chemical & process engineering", artifact: "Shift handover and deviation reports", family: "Classification", win: "Route deviations to the right owner, and surface recurring failure modes hidden in years of free text.", ch: "Prompting", href: "14-prompting.html" },
  { field: "Chemical & process engineering", artifact: "Safety data sheets, permits, patents", family: "Extraction", win: "Pull structured fields — substances, thresholds, exposure limits — into a table you can actually query.", ch: "Prompting", href: "14-prompting.html" },
  { field: "Chemical & process engineering", artifact: "Internal standards and SOPs", family: "Retrieval", win: "Answer 'what is the procedure for X' with the paragraph and document ID that justify the answer.", ch: "RAG", href: "17-rag.html" },

  { field: "Civil & structural engineering", artifact: "Bridge and building inspection reports", family: "Classification", win: "Grade severity consistently across inspectors and decades, then rank what to inspect next.", ch: "Prompting", href: "14-prompting.html" },
  { field: "Civil & structural engineering", artifact: "Tender documents and contracts", family: "Extraction", win: "Extract obligations, penalties and deadlines into a comparable table before you commit to a bid.", ch: "Prompting", href: "14-prompting.html" },
  { field: "Civil & structural engineering", artifact: "Codes, regulations, technical specs", family: "Retrieval", win: "Ask a code question and get the clause — with the citation, because an unsourced answer is worthless here.", ch: "RAG", href: "17-rag.html" },

  { field: "Industrial & manufacturing engineering", artifact: "Maintenance work orders", family: "Classification", win: "Turn a decade of technician free text into failure categories that feed a reliability analysis.", ch: "Prompting", href: "14-prompting.html" },
  { field: "Industrial & manufacturing engineering", artifact: "Supplier emails and quotations", family: "Extraction", win: "Read quotes into structured records — part, quantity, lead time, price — without manual data entry.", ch: "Prompting", href: "14-prompting.html" },
  { field: "Industrial & manufacturing engineering", artifact: "Quality complaints plus production data", family: "Agents", win: "Let the system query the database and run the calculation, then show its work instead of guessing.", ch: "Tools & agents", href: "20-agent-anatomy.html" },

  { field: "Energy & environmental engineering", artifact: "Grid incident and outage logs", family: "Classification", win: "Group causes consistently to find patterns that never fit the dropdown taxonomy.", ch: "Prompting", href: "14-prompting.html" },
  { field: "Energy & environmental engineering", artifact: "Environmental impact assessments", family: "Retrieval", win: "Search hundreds of long PDFs by meaning rather than keyword, with sources attached to every answer.", ch: "RAG", href: "17-rag.html" },
  { field: "Energy & environmental engineering", artifact: "Regulatory filings and reports", family: "Generation", win: "Draft the routine sections from structured data — a human still signs it, but not from a blank page.", ch: "LLM landscape", href: "08-pretraining-landscape.html" },

  { field: "Statistics & social science", artifact: "Open-ended survey responses", family: "Classification", win: "Code thousands of free-text answers into a coding frame, with measurable agreement against human coders.", ch: "Prompting", href: "14-prompting.html" },
  { field: "Statistics & social science", artifact: "Scientific literature for review", family: "Retrieval", win: "Screen abstracts and pull effect sizes and sample sizes at a scale manual review cannot reach.", ch: "RAG", href: "17-rag.html" },
  { field: "Statistics & social science", artifact: "Any of the above, at scale", family: "Evaluation", win: "None of it counts until you can quantify the error rate — treat the model as an instrument to be calibrated.", ch: "Evaluation", href: "19-evaluating-llm-systems.html" }
]
```

```{ojs}
//| echo: false
html`<table style="width:100%; border-collapse:collapse; font-size:0.88rem;">
  <thead>
    <tr style="text-align:left; border-bottom:2px solid #E3E7EC;">
      <th style="padding:0.45rem 0.5rem;">Your documents</th>
      <th style="padding:0.45rem 0.5rem;">Task family</th>
      <th style="padding:0.45rem 0.5rem;">What you get</th>
      <th style="padding:0.45rem 0.5rem;">Chapter</th>
    </tr>
  </thead>
  <tbody>
    ${useCases.filter(d => d.field === fieldChoice).map(d => html`
      <tr style="border-bottom:1px solid #E3E7EC; vertical-align:top;">
        <td style="padding:0.5rem;"><b>${d.artifact}</b></td>
        <td style="padding:0.5rem; white-space:nowrap;">
          <span style="background:#EDF2F2; border:1px solid #E3E7EC; border-radius:0.4em; padding:0.1em 0.45em; font-size:0.82rem;">${d.family}</span>
        </td>
        <td style="padding:0.5rem; color:#5B6272;">${d.win}</td>
        <td style="padding:0.5rem; white-space:nowrap;"><a href="${d.href}">${d.ch} →</a></td>
      </tr>`)}
  </tbody>
</table>`
```
:::

Notice the shape of that list. Almost nothing on it is "build a chatbot".
The valuable applications are not exciting: [reading documents you already
own, at a scale no human can]{.hl}, and producing a table that someone can
act on. That is the work this book prepares you for.

## What did not change

A book that only sells you the upside is a brochure. Four things are as hard
as they ever were, and every one of them will appear in your first project.

**The model does not know what is true.** It produces plausible text, and
plausible text is correct most of the time and confidently wrong some of the
time. There is no internal flag distinguishing the two. Retrieval
(@sec-rag) helps by grounding answers in documents you trust; it does not
eliminate the problem.

**Fluency is not understanding.** ELIZA fooled people in 1966 with a few
dozen rules [@weizenbaum1966eliza], and our instinct to read a mind behind
fluent language has not improved since. Bender and colleagues' *stochastic
parrots* argument [@bender2021stochastic] is the standard statement of the
caution, and it is worth reading even where you disagree with it. Some
dramatic "emergent ability" results also soften considerably once you change
the metric [@schaeffer2023mirage] — a lesson in measurement that
statisticians in particular will enjoy.

**Evaluation is the whole job.** Anyone can demo a prompt that works on
three examples. Knowing that your extraction is 94% accurate — and on which
subgroup it fails — is what separates a project from a party trick. It is
also the least glamorous and most frequently skipped step, which is why it
gets an entire chapter (@sec-llm-eval).

**Your data is still the bottleneck.** Access, quality, privacy, consent,
and the question of whether a given document may leave your building at all.
The model has become the easy part; the pipeline around it has not
(@sec-serving). Nor have the labels: @sec-data-annotation shows a dataset
whose annotators agree 93% of the time and are still barely better than
chance once you correct for how skewed the classes are.

::: {.callout-important appearance="simple"}
Treat a language model the way you would treat a new measuring instrument:
useful, fast, and worth exactly as much as your calibration of it. That
framing will carry you through the rest of this book.
:::

## Three ways to solve the same problem

::: {#gloss-large-language-model-llm .column-margin .term-gloss}
**large language model (LLM)** — a next-word predictor trained on so much
text that it can be steered by a written instruction instead of by
retraining. "Large" is the whole trick, not a detail. [↩](#term-large-language-model-llm){.term-back}
:::

::: {#gloss-fine-tuning .column-margin .term-gloss}
**fine-tuning** — carrying on training a pretrained model on your own,
much smaller, labelled set, so that it specialises without starting from
nothing. [↩](#term-fine-tuning){.term-back}
:::

Newer does not mean better. This book teaches three approaches — counting
words and fitting a classifier, [fine-tuning](#gloss-fine-tuning){#term-fine-tuning .term-ref} a pretrained encoder, and
prompting a [large language model](#gloss-large-language-model-llm){#term-large-language-model-llm .term-ref} — and it teaches all three because each is
still the right answer somewhere. The table below is a preview — every
column gets chapters of its own later, and you are not expected to evaluate
the trade-offs on a first reading. Come back to it when you have a real
problem in front of you.

| | Classical (counts + a classifier) | Fine-tuned encoder | LLM prompting |
|---|---|---|---|
| **Covered in** | @sec-text-as-data, @sec-embeddings | @sec-pretraining | @sec-prompting |
| **Labeled examples needed** | Thousands | Hundreds | [Zero to a handful]{.pro} |
| **Cost per 1,000 documents** | [Effectively nothing]{.pro} | [Effectively nothing]{.pro} after training | [Metered, and it recurs forever]{.con} |
| **Latency** | [Milliseconds on a laptop]{.pro} | Tens of milliseconds on a GPU | [Hundreds of milliseconds to seconds]{.con} |
| **Runs on your own hardware** | [Yes, trivially]{.pro} | [Yes]{.pro} | Only for open-weight models |
| **Explains its decision** | [Yes — you can read the weights]{.pro} | [Not really]{.con} | [No; the stated reason may be invented]{.con} |
| **Handles a task you did not anticipate** | [No — retrain]{.con} | [No — retrain]{.con} | [Yes — rewrite the instruction]{.pro} |
| **Output is reliably structured** | [Yes, by construction]{.pro} | [Yes]{.pro} | [Needs constraining and validation]{.con} |
| **Confidently wrong** | Rarely — it fails visibly | Rarely | [Routinely, and fluently]{.con} |

: The three approaches this book covers {tbl-colwidths="[26,20,18,36]"}

The pattern worth learning now: **the newer approaches buy flexibility
and pay for it in cost, latency, and predictability.** A high-volume,
well-specified, unchanging task — routing ten million tickets into six
categories — is still best served by the oldest technology in the table. A
low-volume task whose definition changes monthly is exactly what prompting
is for.

Most production systems end up combining them, and the combination is
usually the classical model doing the bulk of the work with an LLM handling
the cases it declines.

This table is a claim, and claims in this book get tested. @sec-bake-off puts
the classical end of it on a real Turkish dataset and measures where each
representation wins, including the number of labels at which the ranking
flips. If you only believe one chapter in Part I, make it that one.

## What you need to already know

If you can read a regression table and write a Python loop, you are ready.
@sec-math-refresher collects every mathematical fact this book uses — dot
products, conditional probability, the chain rule, and what "training" means
as optimization. Nothing beyond that is assumed, and no computer-science
background is required.

## How to read this book

**If you are a statistician,** read Parts I and II properly. The payoff is
recognition: embeddings are a factorization, attention is a weighted average
whose weights are estimated, fine-tuning is re-estimation. Then go to
@sec-llm-eval, which is where your training is worth the most and where
most teams are weakest.

**If you are an engineer with a problem due this quarter,** read this
chapter, skim @sec-text-as-data for tokens and cost, then jump to
@sec-prompting and @sec-rag. Come back to Part II when you need to know
*why* something failed rather than merely *that* it did.

**If you are teaching from this,** note that thirty-six chapters across
seven parts is deliberately more than one semester holds. @sec-syllabus maps
the book onto a 13–14 week course and says plainly which chapters get cut.
Parts I–III are the core; Parts V and VI are where you choose. Everything is
CC BY 4.0 — reuse it, translate it, remix it.

## Where to go deeper

A short, personal list. Five good sources beat fifty links.

- **[Speech and Language Processing](https://web.stanford.edu/~jurafsky/slp3/)**,
  Jurafsky & Martin [@jurafsky2025slp]. The field's standard reference, free
  online and continuously updated. When this book is too brief, go here.
- **[NLP Course For You](https://lena-voita.github.io/nlp_course.html)**,
  Elena Voita [@voita2020nlpcourse]. Unusually good visual explanations, and
  the closest in spirit to this book.
- **[The Bitter Lesson](http://www.incompleteideas.net/IncIdeas/BitterLesson.html)**,
  Rich Sutton [@sutton2019bitter]. Six hundred words that explain the last
  seventy years. Worth re-reading once a year.
- **[The AI Index Report](https://hai.stanford.edu/ai-index)**, Stanford HAI
  [@aiindex2026]. When you need a defensible number about capability, cost
  or adoption for a slide, take it from here rather than from a vendor.
- **[Neural Networks: Zero to Hero](https://karpathy.ai/zero-to-hero.html)**,
  Andrej Karpathy. Builds a working language model from scratch, on video.
  The best possible companion to @sec-transformers.
- **[Attention Is All You Need](https://arxiv.org/abs/1706.03762)**
  [@vaswani2017attention]. Read it *after* @sec-transformers, not before —
  it is eight pages and should feel almost obvious by then, which is the
  point.

## Exercises

1. Pick a document type from your own work. Which of the six task families
   applies to it, what would "success" mean as a number, and who is harmed
   by a confident wrong answer?
2. Using the timeline, identify the single milestone whose removal would
   most delay the field's current state. Defend your choice against the two
   most obvious alternatives.
3. The 2003 neural language model and a modern LLM share a structure. Read
   the abstract of @bengio2003neural and list what actually differs. Which
   of those differences are ideas, and which are only scale?
4. Find one public claim about AI capability made in the last month. Locate
   the evaluation underneath it. Does the claim survive contact with it?
