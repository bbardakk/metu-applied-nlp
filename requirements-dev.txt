# Authoring tools. Not needed to render the book, so CI does not install these.
#
#   pip install -r requirements-dev.txt
#   make readability
#
# wordfreq ships large frequency tables and is used only by
# scripts/check-readability.py, which is advisory and never gates a build.
wordfreq>=3.1
